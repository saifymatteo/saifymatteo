# graphify corpus ignores — gitignore spec (merged after .gitignore, so
# these only ever exclude MORE; `!` re-includes are allowed but a parent
# dir exclusion still wins).

# Global
node_modules/
dist/
build/
.vscode/

# NextJS build output + junk design export
nextjs/.next/
nextjs/design/all_svg.json

# Flutter platform scaffolding (generated by `flutter create`, not source)
flutter_web/android/
flutter_web/ios/
flutter_web/linux/
flutter_web/macos/
flutter_web/windows/
flutter_web/web/
flutter_web/.dart_tool/
flutter_web/build/

# Flutter generated code (regenerated by build_runner / flutter_gen — AGENTS.md)
*.g.dart
*.gr.dart
*.gen.dart
*.freezed.dart
app_localizations_*.dart

# Legacy static site assets (binary)
plain_html/media/
