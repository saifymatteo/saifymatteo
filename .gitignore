/.idea/

.vscode/settings.json

*.*~

# graphify knowledge graph — keep only the queryable artifacts for the team,
# ignore everything regenerable (graphify cluster-only / --update rebuild them)
graphify-out/*
!graphify-out/graph.json
!graphify-out/GRAPH_REPORT.md
labels.json

# LLM
.reasonix
.pi
.mcp.json
.cursor/mcp.json
.gemini/settings.json
.kiro/settings/mcp.json
opencode.jsonc
opencode.json
.tempest
.tempest-pid
